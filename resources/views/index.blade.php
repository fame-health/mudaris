<!DOCTYPE html>
<html lang="id">
<head>
  <!-- Basic Meta Tags -->
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">

  <!-- Primary Meta Tags -->
  <title>Mudaris Mandiri Wisata - Paket Umroh Terpercaya & Amanah | 15+ Tahun Pengalaman</title>
  <meta name="title" content="Mudaris Mandiri Wisata - Paket Umroh Terpercaya & Amanah | 15+ Tahun Pengalaman">
  <meta name="description" content="Mudaris Mandiri Wisata menyediakan paket umroh terpercaya dengan 15+ tahun pengalaman. 500+ jamaah puas, pelayanan 24/7, dan harga terjangkau. Daftar sekarang!">
  <meta name="keywords" content="paket umroh, umroh murah, travel umroh terpercaya, paket umroh 2025, umroh keluarga, umroh VIP, agen umroh resmi, Mudaris Mandiri Wisata, umroh jakarta, biaya umroh">
  <meta name="author" content="Mudaris Mandiri Wisata">
  <meta name="robots" content="index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1">
  <meta name="language" content="Indonesian">
  <meta name="revisit-after" content="7 days">

  <!-- Canonical URL -->
  <link rel="canonical" href="https://mudarismandiriwisata.com/">

  <!-- Open Graph / Facebook -->
  <meta property="og:type" content="website">
  <meta property="og:site_name" content="Mudaris Mandiri Wisata">
  <meta property="og:title" content="Mudaris Mandiri Wisata - Paket Umroh Terpercaya & Amanah | 15+ Tahun Pengalaman">
  <meta property="og:description" content="Wujudkan impian ibadah umroh Anda bersama Mudaris Mandiri Wisata. 500+ jamaah puas, pelayanan terpercaya, amanah dan penuh berkah. Konsultasi gratis sekarang!">
  <meta property="og:image" content="https://mudarismandiriwisata.com/images/og-image-mudaris-mandiri-wisata.jpg">
  <meta property="og:image:width" content="1200">
  <meta property="og:image:height" content="630">
  <meta property="og:image:alt" content="Mudaris Mandiri Wisata - Paket Umroh Terpercaya">
  <meta property="og:url" content="https://mudarismandiriwisata.com/">
  <meta property="og:locale" content="id_ID">

  <!-- Twitter Card -->
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:site" content="@mudarismandiriwisata">
  <meta name="twitter:creator" content="@mudarismandiriwisata">
  <meta name="twitter:title" content="Mudaris Mandiri Wisata - Paket Umroh Terpercaya & Amanah">
  <meta name="twitter:description" content="Wujudkan impian ibadah umroh Anda bersama Mudaris Mandiri Wisata. 500+ jamaah puas, pelayanan terpercaya, amanah dan penuh berkah.">
  <meta name="twitter:image" content="https://mudarismandiriwisata.com/images/twitter-card-mudaris-mandiri-wisata.jpg">
  <meta name="twitter:image:alt" content="Mudaris Mandiri Wisata - Paket Umroh Terpercaya">

  <!-- Additional Social Media Meta -->
  <meta property="fb:app_id" content="YOUR_FACEBOOK_APP_ID">
  <meta name="instagram:site" content="@mudarismandiriwisata">

      <!-- FAVICON SECTION untuk Laravel -->
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}">
    <link rel="icon" type="image/png" sizes="16x16" href="{{ asset('favicon-16x16.png') }}">
    <link rel="icon" type="image/png" sizes="32x32" href="{{ asset('favicon-32x32.png') }}">
    <link rel="apple-touch-icon" sizes="180x180" href="{{ asset('apple-touch-icon.png') }}">
    <link rel="icon" type="image/png" sizes="192x192" href="{{ asset('android-chrome-192x192.png') }}">
    <link rel="icon" type="image/png" sizes="512x512" href="{{ asset('android-chrome-512x512.png') }}">
    <link rel="manifest" href="{{ asset('site.webmanifest') }}">

  <!-- Manifest for PWA -->
  <meta name="theme-color" content="#f59e0b">
  <meta name="msapplication-TileColor" content="#f59e0b">

  <!-- DNS Prefetch & Preconnect -->
  <link rel="dns-prefetch" href="//fonts.googleapis.com">
  <link rel="dns-prefetch" href="//cdnjs.cloudflare.com">
  <link rel="preconnect" href="https://fonts.googleapis.com" crossorigin>
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>

  <!-- Security Headers - Enhanced CSP -->
  <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://cdnjs.cloudflare.com https://cdn.tailwindcss.com https://www.googletagmanager.com https://connect.facebook.net; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://cdnjs.cloudflare.com https://cdn.tailwindcss.com; font-src 'self' https://fonts.gstatic.com https://cdnjs.cloudflare.com; img-src 'self' data: https:; connect-src 'self' https:; frame-src 'self' https://www.youtube.com https://www.tiktok.com https://www.instagram.com; media-src 'self' https:; object-src 'none'; base-uri 'self'; form-action 'self' https:;">
  <meta http-equiv="X-Content-Type-Options" content="nosniff">
  <meta http-equiv="X-Frame-Options" content="SAMEORIGIN">
  <meta http-equiv="X-XSS-Protection" content="1; mode=block">
  <meta http-equiv="Referrer-Policy" content="strict-origin-when-cross-origin">

  <!-- Hreflang for International SEO -->
  <link rel="alternate" href="https://mudarismandiriwisata.com/" hreflang="id">
  <link rel="alternate" href="https://mudarismandiriwisata.com/en/" hreflang="en">
  <link rel="alternate" href="https://mudarismandiriwisata.com/" hreflang="x-default">

  <!-- Additional Meta for Better SEO -->
  <meta name="coverage" content="Worldwide">
  <meta name="distribution" content="Global">
  <meta name="rating" content="General">
  <meta name="target" content="all">
  <meta name="HandheldFriendly" content="True">
  <meta name="MobileOptimized" content="320">
  <meta name="mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
  <meta name="apple-mobile-web-app-title" content="Mudaris Mandiri Wisata">

  <!-- Schema.org Structured Data - Travel Agency -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "TravelAgency",
    "name": "Mudaris Mandiri Wisata",
    "alternateName": "Mudaris Mandiri Wisata Indonesia",
    "description": "Agen travel umroh terpercaya dengan 15+ tahun pengalaman, melayani paket umroh untuk seluruh Indonesia",
    "url": "https://mudarismandiriwisata.com/",
    "logo": "https://mudarismandiriwisata.com/assets/images/logo.png",
    "image": "https://mudarismandiriwisata.com/assets/images/logo.png",
    "telephone": "+6285211451111",
    "email": "info@mudarismandiriwisata.com",
    "address": {
      "@type": "PostalAddress",
      "streetAddress": "Jl. Kubang Raya Km. 1.5 simpang empat panam, tuah karya, kec. Tampan",
      "addressLocality": "Pekanbaru",
      "addressRegion": "Riau",
      "postalCode": "28293",
      "addressCountry": "ID"
    },
    "geo": {
      "@type": "GeoCoordinates",
      "latitude": "0.4574",
      "longitude": "101.3431"
    },
    "openingHours": [
      "Mo-Fr 08:00-17:00",
      "Sa 08:00-12:00"
    ],
    "sameAs": [
      "https://web.facebook.com/mmw.umrahdanhaji",
      "https://www.instagram.com/mudarismandiriwisata_mmw/",
      "https://www.youtube.com/@mudariswisata",
      "https://www.tiktok.com/@mmw_mudariswisata"
    ],
    "hasOfferCatalog": {
      "@type": "OfferCatalog",
      "name": "Paket Umroh Mudaris Mandiri Wisata",
      "itemListElement": [
        {
          "@type": "Offer",
          "itemOffered": {
            "@type": "TouristTrip",
            "name": "Paket Umroh Regular",
            "description": "Paket umroh ekonomis dengan fasilitas lengkap"
          }
        },
        {
          "@type": "Offer",
          "itemOffered": {
            "@type": "TouristTrip",
            "name": "Paket Umroh Family",
            "description": "Paket umroh khusus keluarga dengan pelayanan terbaik"
          }
        },
        {
          "@type": "Offer",
          "itemOffered": {
            "@type": "TouristTrip",
            "name": "Paket Umroh VIP",
            "description": "Paket umroh premium dengan fasilitas mewah"
          }
        }
      ]
    },
    "aggregateRating": {
      "@type": "AggregateRating",
      "ratingValue": "4.8",
      "reviewCount": "500",
      "bestRating": "5",
      "worstRating": "1"
    },
    "founder": {
      "@type": "Person",
      "name": "Founder Mudaris Mandiri Wisata"
    }
  }
  </script>

  <!-- Local Business Schema -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "LocalBusiness",
    "name": "Mudaris Mandiri Wisata",
    "description": "Agen travel umroh terpercaya dan amanah",
    "url": "https://mudarismandiriwisata.com/",
    "telephone": "+6285211451111",
    "address": {
      "@type": "PostalAddress",
      "streetAddress": "Jl. Kubang Raya Km. 1.5 simpang empat panam, tuah karya, kec. Tampan",
      "addressLocality": "Pekanbaru",
      "addressRegion": "Riau",
      "postalCode": "28293",
      "addressCountry": "ID"
    },
    "priceRange": "$$",
    "openingHours": [
      "Mo-Fr 08:00-17:00",
      "Sa 08:00-12:00"
    ],
    "geo": {
      "@type": "GeoCoordinates",
      "latitude": "0.4574",
      "longitude": "101.3431"
    },
    "acceptsReservations": true,
    "paymentAccepted": "Cash, Credit Card, Bank Transfer"
  }
  </script>

  <!-- Organization Schema -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "Organization",
    "name": "Mudaris Mandiri Wisata",
    "legalName": "PT. Mudaris Mandiri Wisata Indonesia",
    "url": "https://mudarismandiriwisata.com/",
    "logo": "https://mudarismandiriwisata.com/images/logo.png",
    "foundingDate": "2012",
    "founders": [
      {
        "@type": "Person",
        "name": "Founder Name"
      }
    ],
    "address": {
      "@type": "PostalAddress",
      "streetAddress": "Jl. Kubang Raya Km. 1.5 simpang empat panam, tuah karya, kec. Tampan",
      "addressLocality": "Pekanbaru",
      "addressRegion": "Riau",
      "postalCode": "28293",
      "addressCountry": "ID"
    },
    "contactPoint": {
      "@type": "ContactPoint",
      "contactType": "Customer Service",
      "telephone": "+6285211451111",
      "email": "cs@mudarismandiriwisata.com",
      "availableLanguage": "Indonesian"
    },
    "sameAs": [
      "https://web.facebook.com/mmw.umrahdanhaji",
      "https://www.instagram.com/mudarismandiriwisata_mmw/",
      "https://www.youtube.com/@mudariswisata",
      "https://www.tiktok.com/@mmw_mudariswisata"
    ]
  }
  </script>

  <!-- FAQ Schema -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "FAQPage",
    "mainEntity": [
      {
        "@type": "Question",
        "name": "Apakah Mudaris Mandiri Wisata sudah berizin resmi?",
        "acceptedAnswer": {
          "@type": "Answer",
          "text": "Ya, Mudaris Mandiri Wisata telah berizin resmi dan berpengalaman lebih dari 15 tahun dalam melayani jamaah umroh."
        }
      },
      {
        "@type": "Question",
        "name": "Berapa lama paket umroh yang tersedia?",
        "acceptedAnswer": {
          "@type": "Answer",
          "text": "Kami menyediakan berbagai paket umroh mulai dari 9 hari hingga 14 hari dengan pilihan hotel 3-5 bintang."
        }
      },
      {
        "@type": "Question",
        "name": "Apakah ada garansi keberangkatan?",
        "acceptedAnswer": {
          "@type": "Answer",
          "text": "Ya, kami memberikan garansi keberangkatan 100% untuk semua paket yang telah dibayar lunas."
        }
      },
      {
        "@type": "Question",
        "name": "Bagaimana cara melakukan pembayaran paket umroh?",
        "acceptedAnswer": {
          "@type": "Answer",
          "text": "Pembayaran dapat dilakukan secara tunai, transfer bank, atau cicilan sesuai dengan ketentuan yang berlaku."
        }
      }
    ]
  }
  </script>

  <!-- Google Analytics 4 (Placeholder for GA4 ID) -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXXXX"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-XXXXXXXXXX');
  </script>

  <!-- Google Tag Manager (Placeholder for GTM ID) -->
  <script>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
  new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
  j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
  'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
  })(window,document,'script','dataLayer','GTM-XXXXXXX');</script>

  <!-- Facebook Pixel (Placeholder for Pixel ID) -->
  <script>
    !function(f,b,e,v,n,t,s)
    {if(f.fbq)return;n=f.fbq=function(){n.callMethod?
    n.callMethod.apply(n,arguments):n.queue.push(arguments)};
    if(!f._fbq)f._fbq=n;n.push=n;n.loaded=!0;n.version='2.0';
    n.queue=[];t=b.createElement(e);t.async=true;
    t.src=v;s=b.getElementsByTagName(e)[0];
    s.parentNode.insertBefore(t,s)}(window, document,'script',
    'https://connect.facebook.net/en_US/fbevents.js');
    fbq('init', 'YOUR_PIXEL_ID');
    fbq('track', 'PageView');
  </script>
  <noscript><img height="1" width="1" style="display:none"
    src="https://www.facebook.com/tr?id=YOUR_PIXEL_ID&ev=PageView&noscript=1"
  /></noscript>

  <!-- Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- Additional SEO optimizations -->
  <link rel="sitemap" type="application/xml" href="/sitemap.xml">
  <link rel="alternate" type="application/rss+xml" title="Mudaris Mandiri Wisata RSS Feed" href="/feed.xml">

 
<!-- Font Awesome 6 (Latest) -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800&display=swap');

 * {
  font-family: 'Poppins', sans-serif;
}

/* Warna utama biru (gradient identik biru) */
.gradient-bg {
  background: linear-gradient(135deg, #0052CC 0%, #0ea5e9 50%, #6366f1 100%);
}

/* Hero section biru muda */
.hero-gradient {
  background: linear-gradient(135deg, #dbeafe 0%, #93c5fd 50%, #60a5fa 100%);
}

/* Hover effect card */
.card-hover {
  transition: all 0.3s ease;
}

.card-hover:hover {
  transform: translateY(-10px);
  box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
}

/* Floating animation */
.floating-animation {
  animation: float 3s ease-in-out infinite;
}

@keyframes float {
  0%, 100% { transform: translateY(0px); }
  50% { transform: translateY(-10px); }
}

/* Fade-in animation */
.fade-in {
  opacity: 0;
  transform: translateY(30px);
  transition: all 0.6s ease;
}

.fade-in.visible {
  opacity: 1;
  transform: translateY(0);
}

/* Glassmorphism effect */
.glass-morphism {
  background: rgba(255, 255, 255, 0.25);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.18);
}

/* Text shadow ringan */
.text-shadow {
  text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.1);
}

/* Blob morphing effect */
.blob {
  border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%;
  animation: blob 7s ease-in-out infinite;
}

@keyframes blob {
  0%, 100% { border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%; }
  25% { border-radius: 58% 42% 75% 25% / 76% 46% 54% 24%; }
  50% { border-radius: 50% 50% 33% 67% / 55% 27% 73% 45%; }
  75% { border-radius: 33% 67% 58% 42% / 63% 68% 32% 37%; }
}

/* Mobile menu animation */
#mobile-menu {
  transition: all 0.3s ease;
  max-height: 0;
  overflow: hidden;
}

#mobile-menu.active {
  max-height: 500px;
}

/* Icon rotation for mobile */
.mobile-menu-icon {
  transition: transform 0.3s ease;
}

.mobile-menu-icon.active {
  transform: rotate(90deg);
}
/* Keyframe untuk transisi masuk & keluar */
@keyframes slideShow {
    0%, 33.33%, 100% {
        opacity: 0;
    }
    10%, 23.33% {
        opacity: 1;
    }
}

/* Animasi dengan delay untuk masing-masing slide */
.absolute.inset-0.bg-cover:nth-child(2) {
    animation: slideShow 12s infinite;
}
.absolute.inset-0.bg-cover:nth-child(3) {
    animation: slideShow 12s infinite;
    animation-delay: 4s;
}
.absolute.inset-0.bg-cover:nth-child(4) {
    animation: slideShow 12s infinite;
    animation-delay: 8s;
}


  </style>
</head>
<body class="bg-gray-50 text-gray-800 overflow-x-hidden">ty

  <!-- Navbar -->
  <nav class="glass-morphism fixed w-full z-50 top-0 transition-all duration-300" id="navbar">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between h-20 items-center">
        <!-- Logo -->
<!-- Logo -->
<div class="flex-shrink-0 flex items-center space-x-3">
  <div class=" items-center justify-center">
    <img src="{{ asset('assets/images/logo.png') }}" alt="Logo" class="w-40 h-26 object-contain" />
  </div>
</div>

             <!-- Desktop Menu -->
      <div class="hidden md:flex space-x-8">
        <a href="#home" class="relative text-gray-700 hover:text-yellow-600 transition-colors duration-300 group">
          Beranda
          <span class="absolute -bottom-1 left-0 w-0 h-0.5 bg-yellow-600 transition-all duration-300 group-hover:w-full"></span>
        </a>
        <a href="#paket" class="relative text-gray-700 hover:text-yellow-600 transition-colors duration-300 group">
          Paket Umroh
          <span class="absolute -bottom-1 left-0 w-0 h-0.5 bg-yellow-600 transition-all duration-300 group-hover:w-full"></span>
        </a>
        <a href="#galeri" class="relative text-gray-700 hover:text-yellow-600 transition-colors duration-300 group">
          Galeri
          <span class="absolute -bottom-1 left-0 w-0 h-0.5 bg-yellow-600 transition-all duration-300 group-hover:w-full"></span>
        </a>
        <a href="#testimoni" class="relative text-gray-700 hover:text-yellow-600 transition-colors duration-300 group">
          Testimoni
          <span class="absolute -bottom-1 left-0 w-0 h-0.5 bg-yellow-600 transition-all duration-300 group-hover:w-full"></span>
        </a>
        <a href="#kontak" class="relative text-gray-700 hover:text-yellow-600 transition-colors duration-300 group">
          Kontak
          <span class="absolute -bottom-1 left-0 w-0 h-0.5 bg-yellow-600 transition-all duration-300 group-hover:w-full"></span>
        </a>
      </div>



      <!-- CTA Desktop -->
      <div class="hidden md:flex">
        <button class="gradient-bg text-white px-6 py-2 rounded-full font-semibold hover:shadow-lg transform hover:scale-105 transition-all duration-300">
          Konsultasi Gratis
        </button>
      </div>

      <!-- Mobile Menu Button -->
      <div class="md:hidden">
        <button id="menu-btn" class="text-gray-700 hover:text-yellow-600 transition-colors">
          <i class="fas fa-bars text-2xl"></i>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile Menu -->
  <div id="mobile-menu" class="md:hidden hidden glass-morphism absolute top-20 left-0 right-0 w-full z-50">
    <div class="px-4 pt-2 pb-6 space-y-4 bg-white shadow-md rounded-b-xl">
      <a href="#home" class="block py-3 px-4 text-gray-700 hover:bg-yellow-50 rounded-lg transition-colors">Beranda</a>
      <a href="#paket" class="block py-3 px-4 text-gray-700 hover:bg-yellow-50 rounded-lg transition-colors">Paket Umroh</a>
      <a href="#galeri" class="block py-3 px-4 text-gray-700 hover:bg-yellow-50 rounded-lg transition-colors">Galeri</a>
      <a href="#testimoni" class="block py-3 px-4 text-gray-700 hover:bg-yellow-50 rounded-lg transition-colors">Testimoni</a>
      <a href="#kontak" class="block py-3 px-4 text-gray-700 hover:bg-yellow-50 rounded-lg transition-colors">Kontak</a>
      <button class="w-full gradient-bg text-white px-6 py-3 rounded-full font-semibold hover:shadow-lg transition-all duration-300">
        Konsultasi Gratis
      </button>
    </div>
  </div>
</nav>



  <!-- Hero Section -->
<section id="home" class="relative min-h-screen flex items-center justify-center overflow-hidden">
    <!-- Background Image Carousel -->
<div class="absolute inset-0 z-0">
  <div class="relative w-full h-full">

    <!-- Slide 1 - Masjid al-Haram -->
    <div class="absolute inset-0 bg-black opacity-40"></div>
    <div class="absolute inset-0 bg-cover bg-center transition-opacity duration-1000 opacity-100"
         style="background-image: url('https://images.unsplash.com/photo-1591604129939-f1efa4d9f7fa?ixlib=rb-4.0.3&auto=format&fit=crop&w=1950&q=80')">
    </div>

    <!-- Slide 2 - Ka'bah di Mekkah saat senja -->
    <div class="absolute inset-0 bg-cover bg-center transition-opacity duration-1000 opacity-0"
         style="background-image: url('https://images.pexels.com/photos/18274181/pexels-photo-18274181.jpeg')">
    </div>


  </div>
</div>


    <!-- Decorative Blobs -->
    <div class="absolute top-20 right-20 w-64 h-64 bg-blue-300 opacity-20 blob"></div>
    <div class="absolute bottom-20 left-20 w-48 h-48 bg-blue-400 opacity-20 blob" style="animation-delay: -2s;"></div>

    <div class="relative z-10 text-center px-4 max-w-6xl mx-auto">
      <div class="fade-in">
        <h1 class="text-5xl md:text-7xl font-bold mb-6 text-shadow">
          <span class="bg-gradient-to-r from-blue-600 via-blue-500 to-blue-700 bg-clip-text text-transparent">
            Mudaris Mandiri Wisata
          </span>
        </h1>
        <p class="text-xl md:text-2xl mb-8 text-white max-w-3xl mx-auto leading-relaxed">
          Wujudkan impian ibadah Umroh Anda bersama kami. Pelayanan terpercaya, amanah, dan penuh berkah.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
          <button class="gradient-bg text-white px-8 py-4 rounded-full font-semibold text-lg hover:shadow-xl transform hover:scale-105 transition-all duration-300 floating-animation">
            <i class="fas fa-plane mr-2"></i>
            Lihat Paket Umroh
          </button>
          <button class="border-2 border-blue-500 text-blue-500 px-8 py-4 rounded-full font-semibold text-lg hover:bg-blue-500 hover:text-white transition-all duration-300 bg-white bg-opacity-20 hover:bg-opacity-100">
            <i class="fas fa-phone mr-2"></i>
            Hubungi Kami
          </button>
        </div>
      </div>
    </div>

    <!-- Scroll Indicator -->
    <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
      <div class="w-6 h-10 border-2 border-blue-500 rounded-full flex justify-center">
        <div class="w-1 h-3 bg-blue-500 rounded-full mt-2 animate-pulse"></div>
      </div>
    </div>
</section>

  <!-- Stats Section -->
  <section class="py-16 gradient-bg">
    <div class="max-w-6xl mx-auto px-4">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-8 text-center text-white">
        <div class="fade-in">
          <div class="text-4xl font-bold mb-2">500+</div>
          <div class="text-lg opacity-90">Jamaah Puas</div>
        </div>
        <div class="fade-in">
          <div class="text-4xl font-bold mb-2">50+</div>
          <div class="text-lg opacity-90">Keberangkatan</div>
        </div>
        <div class="fade-in">
          <div class="text-4xl font-bold mb-2">15</div>
          <div class="text-lg opacity-90">Tahun Pengalaman</div>
        </div>
        <div class="fade-in">
          <div class="text-4xl font-bold mb-2">100%</div>
          <div class="text-lg opacity-90">Amanah</div>
        </div>
      </div>
    </div>
  </section>

<!-- Paket Umroh -->
<!-- Paket Umroh -->
<section id="paket" class="py-20 bg-white">
  <div class="max-w-6xl mx-auto px-4">
    <div class="text-center mb-16 fade-in">
      <h2 class="text-4xl font-bold mb-4 bg-gradient-to-r from-yellow-600 to-orange-600 bg-clip-text text-transparent">
        Paket Umroh Pilihan
      </h2>
      <p class="text-xl text-gray-600 max-w-2xl mx-auto">
        Pilih paket yang sesuai dengan kebutuhan dan budget Anda
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      @foreach(App\Models\Paket::active()->get() as $paket)
      @php
        // Pastikan gradient_colors adalah array dengan nilai default
        $gradientColors = is_array($paket->gradient_colors) ? $paket->gradient_colors : ['from' => 'blue-500', 'to' => 'purple-500'];
        // Pastikan features adalah array
        $features = is_array($paket->features) ? $paket->features : [];

        // Definisikan warna berdasarkan kategori
        $categoryColors = match($paket->category) {
            'vip' => [
                'gradient' => ['from' => 'yellow-400', 'to' => 'orange-500'],
                'border' => 'border-yellow-200',
                'bg_accent' => 'bg-yellow-50',
                'text_accent' => 'text-yellow-600',
                'icon_color' => 'text-yellow-500',
                'button_bg' => 'bg-gradient-to-r from-yellow-400 to-orange-500',
                'price_color' => 'text-yellow-600',
                'tag_bg' => 'bg-yellow-500',
                'category_name' => 'VIP'
            ],
            'family' => [
                'gradient' => ['from' => 'green-400', 'to' => 'blue-500'],
                'border' => 'border-green-200',
                'bg_accent' => 'bg-green-50',
                'text_accent' => 'text-green-600',
                'icon_color' => 'text-green-500',
                'button_bg' => 'bg-gradient-to-r from-green-400 to-blue-500',
                'price_color' => 'text-green-600',
                'tag_bg' => 'bg-green-500',
                'category_name' => 'FAMILY'
            ],
            default => [ // Regular
                'gradient' => ['from' => 'blue-400', 'to' => 'purple-500'],
                'border' => 'border-blue-200',
                'bg_accent' => 'bg-blue-50',
                'text_accent' => 'text-blue-600',
                'icon_color' => 'text-blue-500',
                'button_bg' => 'bg-gradient-to-r from-blue-400 to-purple-500',
                'price_color' => 'text-blue-600',
                'tag_bg' => 'bg-blue-500',
                'category_name' => 'REGULAR'
            ]
        };
      @endphp

      <div class="card-hover bg-white rounded-3xl p-8 shadow-lg relative overflow-hidden fade-in border-2
        {{ $paket->is_featured ? 'transform scale-105 border-yellow-400' : $categoryColors['border'] }}">

        <!-- Category Badge -->
        <div class="absolute top-4 left-4 {{ $categoryColors['tag_bg'] }} text-white px-3 py-1 rounded-full text-xs font-bold">
          {{ $categoryColors['category_name'] }}
        </div>

        @if($paket->is_featured)
        <div class="absolute top-0 right-0 bg-yellow-400 text-white px-4 py-2 rounded-bl-2xl font-semibold">
          POPULER
        </div>
        @else
        <div class="absolute top-0 right-0 w-20 h-20 bg-gradient-to-br from-{{ $categoryColors['gradient']['from'] }} to-{{ $categoryColors['gradient']['to'] }} rounded-bl-3xl"></div>
        @endif

        <div class="relative">
          <!-- Image Container -->
<div class="max-w-[1587px] w-full aspect-[1587/2245] mx-auto rounded-xl overflow-hidden mt-8 border shadow">
  <img src="{{ asset($paket->main_image) }}"
       alt="{{ htmlspecialchars($paket->name, ENT_QUOTES) }}"
       class="w-full h-full object-cover">
</div>


          <div class="w-16 h-16 bg-gradient-to-br from-{{ $categoryColors['gradient']['from'] }} to-{{ $categoryColors['gradient']['to'] }} rounded-2xl flex items-center justify-center mb-6 absolute -top-8 left-6 shadow-md">
            <i class="fas fa-{{ $paket->icon }} text-white text-2xl"></i>
          </div>

          <h3 class="text-2xl font-bold mb-4 text-gray-800">{{ htmlspecialchars($paket->name, ENT_QUOTES) }}</h3>
          <p class="text-gray-600 mb-4">{{ htmlspecialchars($paket->description, ENT_QUOTES) }}</p>

          <!-- Hotel Rating dan Durasi -->
          <div class="flex items-center justify-between mb-6 p-3 {{ $categoryColors['bg_accent'] }} rounded-lg border border-opacity-20 {{ $categoryColors['border'] }}">
            <!-- Hotel Rating -->
            <div class="flex items-center">
              <i class="fas fa-hotel {{ $categoryColors['icon_color'] }} mr-2"></i>
              <span class="text-sm text-gray-600 mr-2">Hotel:</span>
              <div class="flex items-center">
                @for($i = 1; $i <= 5; $i++)
                  @if($i <= ($paket->hotel_rating ?? 4))
                    <i class="fas fa-star text-yellow-400 text-sm"></i>
                  @else
                    <i class="far fa-star text-gray-300 text-sm"></i>
                  @endif
                @endfor
                <span class="ml-1 text-sm {{ $categoryColors['text_accent'] }} font-semibold">({{ $paket->hotel_rating ?? 4 }})</span>
              </div>
            </div>

            <!-- Durasi -->
            <div class="flex items-center">
              <i class="fas fa-calendar-alt {{ $categoryColors['icon_color'] }} mr-2"></i>
              <span class="text-sm font-semibold {{ $categoryColors['text_accent'] }}">
                {{ $paket->duration_days ?? 14 }} Hari
              </span>
            </div>
          </div>

          <!-- Tanggal Keberangkatan -->
          @if($paket->departure_date)
          <div class="flex items-center mb-4 p-3 {{ $categoryColors['bg_accent'] }} rounded-lg border border-opacity-20 {{ $categoryColors['border'] }}">
            <i class="fas fa-plane-departure {{ $categoryColors['icon_color'] }} mr-2"></i>
            <span class="text-sm text-gray-600">Keberangkatan:</span>
            <span class="ml-2 text-sm font-semibold {{ $categoryColors['text_accent'] }}">
              {{ \Carbon\Carbon::parse($paket->departure_date)->format('d M Y') }}
            </span>
          </div>
          @endif

          <ul class="space-y-2 mb-8">
            @foreach($features as $feature)
            <li class="flex items-center text-gray-600">
              <i class="fas fa-check {{ $categoryColors['icon_color'] }} mr-3"></i>
              {{ is_array($feature) ? implode(', ', $feature) : htmlspecialchars($feature, ENT_QUOTES) }}
            </li>
            @endforeach
          </ul>

          <!-- Harga -->
          <div class="text-center mb-4 p-4 {{ $categoryColors['bg_accent'] }} rounded-lg border border-opacity-20 {{ $categoryColors['border'] }}">
            <div class="text-sm text-gray-600 mb-1">Mulai dari</div>
            <div class="text-2xl font-bold {{ $categoryColors['price_color'] }}">
              {{ htmlspecialchars($paket->formatted_price, ENT_QUOTES) }}
            </div>
            <div class="text-xs text-gray-500">per orang</div>
          </div>

          <!-- Button Full Width -->
          <button onclick="sendWhatsApp('{{ $paket->name }}', '{{ $paket->formatted_price }}', '{{ $paket->duration_days ?? 14 }}', '{{ $paket->hotel_rating ?? 4 }}', '{{ $categoryColors['category_name'] }}')"
                  class="w-full {{ $categoryColors['button_bg'] }} text-white py-3 px-6 rounded-xl font-semibold hover:shadow-lg transition-all duration-300 hover:scale-105 flex items-center justify-center">
            <i class="fab fa-whatsapp mr-2 text-lg"></i>
            <span>Pilih Paket Ini</span>
          </button>
        </div>
      </div>
      @endforeach
    </div>

    @if(App\Models\Paket::active()->count() === 0)
    <div class="text-center py-12">
      <p class="text-gray-500">Tidak ada paket yang tersedia saat ini</p>
    </div>
    @endif
  </div>
</section>

<script>
function sendWhatsApp(packageName, price, duration, hotelRating, category) {
    // Nomor WhatsApp tujuan
    const phoneNumber = '6285211451111'; // Format internasional Indonesia

    // Template pesan yang akan dikirim
    const message = `🕌 *INQUIRY PAKET UMROH* 🕌

Assalamualaikum, saya tertarik dengan:

📦 *Paket*: ${packageName}
🏷️ *Kategori*: ${category}
💰 *Harga*: ${price}
📅 *Durasi*: ${duration} hari
🏨 *Hotel Rating*: ${hotelRating}⭐

Mohon informasi lebih lanjut, terima kasih.`;

    // Encode pesan untuk URL
    const encodedMessage = encodeURIComponent(message);

    // Buat URL WhatsApp
    const url = `https://wa.me/${phoneNumber}?text=${encodedMessage}`;

    // Arahkan pengguna ke WhatsApp
    window.open(url, '_blank');
}
</script>


  <!-- Galeri -->
<section id="galeri" class="py-20 bg-gradient-to-br from-gray-50 to-gray-100">
    <div class="max-w-6xl mx-auto px-4">
        <div class="text-center mb-16 fade-in">
            <h2 class="text-4xl font-bold mb-4 bg-gradient-to-r from-yellow-600 to-orange-600 bg-clip-text text-transparent">
                Galeri Perjalanan
            </h2>
            <p class="text-xl text-gray-600">Dokumentasi perjalanan jamaah bersama Mudaris Mandiri Wisata</p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            @foreach(App\Models\Gallery::all() as $gallery)
            <div class="fade-in">
                <div class="group relative overflow-hidden rounded-2xl shadow-lg hover:shadow-2xl transition-all duration-300 transform hover:scale-105">
                    <img src="{{ asset('storage/' . $gallery->image) }}"
                         class="w-full h-64 object-cover group-hover:scale-110 transition-transform duration-300"
                         alt="{{ $gallery->title }}" />
                    <div class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
                    <div class="absolute bottom-4 left-4 text-white opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                        <h3 class="text-lg font-semibold">{{ $gallery->title }}</h3>
                        <p class="text-sm opacity-90">{{ $gallery->location }}</p>
                    </div>
                </div>
            </div>
            @endforeach
        </div>

        @if(App\Models\Gallery::count() === 0)
        <div class="text-center py-12">
            <p class="text-gray-500">Galeri belum tersedia</p>
        </div>
        @endif
    </div>
</section>

<!-- Media Section -->
<!-- Media Section -->
<section id="{{ $section->id ?? 'media' }}" class="py-20 {{ $section->background_color ?? 'bg-gradient-to-br from-gray-50 to-gray-100' }}">
    <div class="max-w-6xl mx-auto px-4">
        <div class="text-center mb-12 fade-in">
            <h2 class="text-4xl font-bold mb-4 {{ $section->title_color ?? 'bg-gradient-to-r from-yellow-600 to-orange-600 bg-clip-text text-transparent' }}">
                {{ $section->title ?? 'Dokumentasi & Media Sosial' }}
            </h2>
            @if(!empty($section->subtitle))
            <p class="text-xl {{ $section->subtitle_color ?? 'text-gray-600' }}">
                {{ $section->subtitle ?? 'Poster kegiatan, video TikTok, dan postingan Instagram terbaru' }}
            </p>
            @endif
        </div>

        @php
            $mediaItems = App\Models\Media::where('is_active', 1)
                ->orderBy('order')
                ->limit($section->item_limit ?? 3)
                ->get();
        @endphp

        <div class="grid grid-cols-1 md:grid-cols-{{ $section->columns ?? 3 }} gap-6 fade-in">
            @foreach($mediaItems as $media)
                <div class="bg-white rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300 transform hover:scale-105">
                    <!-- Container dengan aspect ratio 9:16 untuk semua jenis media -->
                    <div class="relative w-full aspect-[9/16] bg-gray-100 overflow-hidden">
                        @if($media->type === 'poster')
                            <!-- Poster / Banner -->
                            <img src="{{ $media->image_url ?? 'https://via.placeholder.com/400x320?text=Poster+Rahmah+Travel' }}"
                                 alt="{{ $media->alt_text ?? 'Poster Mudaris Mandiri Wisata' }}"
                                 class="w-full h-full object-cover" />

                        @elseif($media->type === 'youtube')
                            <!-- YouTube Embed dengan thumbnail dan tombol play -->
                            <div class="relative w-full h-full group cursor-pointer" onclick="loadYouTubeVideo(this, '{{ $media->embed_id }}')">
                                <!-- Thumbnail YouTube -->
                                <img src="https://img.youtube.com/vi/{{ $media->embed_id }}/maxresdefault.jpg"
                                     alt="YouTube Video Thumbnail"
                                     class="w-full h-full object-cover" />

                                <!-- Overlay dengan tombol play -->
                                <div class="absolute inset-0 bg-black bg-opacity-30 flex items-center justify-center group-hover:bg-opacity-40 transition-all duration-200">
                                    <div class="bg-red-600 rounded-full p-3 shadow-lg transform group-hover:scale-110 transition-transform duration-200">
                                        <svg class="w-8 h-8 text-white ml-1" fill="currentColor" viewBox="0 0 24 24">
                                            <path d="M8 5v14l11-7z"/>
                                        </svg>
                                    </div>
                                </div>

                                <!-- Label YouTube -->
                                <div class="absolute top-2 right-2 bg-red-600 text-white px-2 py-1 rounded text-xs font-medium">
                                    YouTube
                                </div>
                            </div>

                        @elseif($media->type === 'tiktok')
                            <!-- TikTok Embed dengan thumbnail dan tombol play -->
                            <div class="relative w-full h-full group cursor-pointer" onclick="loadTikTokVideo(this, '{{ $media->embed_id }}')">
                                <!-- Placeholder untuk TikTok (karena tidak ada thumbnail API yang mudah) -->
                                <div class="w-full h-full bg-gradient-to-br from-pink-400 to-purple-600 flex items-center justify-center">
                                    <div class="text-center text-white">
                                        <svg class="w-16 h-16 mx-auto mb-2" fill="currentColor" viewBox="0 0 24 24">
                                            <path d="M19.59 6.69a4.83 4.83 0 0 1-3.77-4.25V2h-3.45v13.67a2.89 2.89 0 0 1-5.2 1.74 2.89 2.89 0 0 1 2.31-4.64 2.93 2.93 0 0 1 .88.13V9.4a6.84 6.84 0 0 0-.88-.05A6.33 6.33 0 0 0 5 20.1a6.34 6.34 0 0 0 10.86-4.43V7a8.16 8.16 0 0 0 4.77 1.52v-3.4a4.85 4.85 0 0 1-1.04.57z"/>
                                        </svg>
                                        <p class="text-sm font-medium">Klik untuk memutar</p>
                                    </div>
                                </div>

                                <!-- Overlay dengan tombol play -->
                                <div class="absolute inset-0 bg-black bg-opacity-20 flex items-center justify-center group-hover:bg-opacity-30 transition-all duration-200">
                                    <div class="bg-black bg-opacity-60 rounded-full p-3 shadow-lg transform group-hover:scale-110 transition-transform duration-200">
                                        <svg class="w-8 h-8 text-white ml-1" fill="currentColor" viewBox="0 0 24 24">
                                            <path d="M8 5v14l11-7z"/>
                                        </svg>
                                    </div>
                                </div>

                                <!-- Label TikTok -->
                                <div class="absolute top-2 right-2 bg-black text-white px-2 py-1 rounded text-xs font-medium">
                                    TikTok
                                </div>
                            </div>

                        @elseif($media->type === 'instagram')
                            <!-- Instagram dengan thumbnail dan tombol -->
                            <div class="relative w-full h-full group cursor-pointer" onclick="loadInstagramPost(this, '{{ $media->embed_id }}')">
                                <!-- Placeholder untuk Instagram -->
                                <div class="w-full h-full bg-gradient-to-br from-purple-500 to-pink-500 flex items-center justify-center">
                                    <div class="text-center text-white">
                                        <svg class="w-16 h-16 mx-auto mb-2" fill="currentColor" viewBox="0 0 24 24">
                                            <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/>
                                        </svg>
                                        <p class="text-sm font-medium">Lihat Postingan</p>
                                    </div>
                                </div>

                                <!-- Label Instagram -->
                                <div class="absolute top-2 right-2 bg-gradient-to-r from-purple-500 to-pink-500 text-white px-2 py-1 rounded text-xs font-medium">
                                    Instagram
                                </div>
                            </div>
                        @endif
                    </div>

                    <!-- Caption Area -->
                    @if(!empty($media->title) || !empty($media->alt_text))
                        <div class="p-4 {{ $section->caption_background ?? 'bg-white' }}">
                            @if(!empty($media->title))
                                <h3 class="font-semibold {{ $section->caption_text_color ?? 'text-gray-800' }} mb-1">{{ $media->title }}</h3>
                            @endif
                            @if(!empty($media->alt_text))
                                <p class="text-sm {{ $section->caption_text_color ?? 'text-gray-600' }}">{{ $media->alt_text }}</p>
                            @endif
                        </div>
                    @endif
                </div>
            @endforeach
        </div>

        @if($mediaItems->isEmpty())
            <div class="text-center py-8">
                <p class="{{ $section->empty_text_color ?? 'text-gray-500' }}">
                    {{ $section->empty_text ?? 'Tidak ada media yang tersedia saat ini.' }}
                </p>
            </div>
        @endif

        @if(!empty($section->button_text))
        <div class="text-center mt-8">
            <a href="{{ $section->button_link ?? '#' }}"
               class="inline-block px-6 py-3 rounded-lg {{ $section->button_color ?? 'bg-orange-500 hover:bg-orange-600' }} text-white font-medium transition-colors">
                {{ $section->button_text }}
            </a>
        </div>
        @endif
    </div>
</section>

<script>
// Function untuk load YouTube video
function loadYouTubeVideo(element, videoId) {
    const iframe = document.createElement('iframe');
    iframe.src = `https://www.youtube.com/embed/${videoId}?autoplay=1`;
    iframe.className = 'w-full h-full';
    iframe.frameBorder = '0';
    iframe.allowFullscreen = true;
    iframe.allow = 'accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture';

    element.innerHTML = '';
    element.appendChild(iframe);
}

// Function untuk load TikTok video
function loadTikTokVideo(element, videoId) {
    const iframe = document.createElement('iframe');
    iframe.src = `https://www.tiktok.com/embed/${videoId}`;
    iframe.className = 'w-full h-full';
    iframe.frameBorder = '0';
    iframe.allowFullscreen = true;
    iframe.allow = 'autoplay; encrypted-media';

    element.innerHTML = '';
    element.appendChild(iframe);
}

// Function untuk load Instagram post
function loadInstagramPost(element, postId) {
    const iframe = document.createElement('iframe');
    iframe.src = `https://www.instagram.com/p/${postId}/embed`;
    iframe.className = 'w-full h-full';
    iframe.frameBorder = '0';
    iframe.scrolling = 'no';
    iframe.allowTransparency = true;

    element.innerHTML = '';
    element.appendChild(iframe);
}
</script>





<!-- Testimoni -->
<!-- Testimoni -->
<section id="testimoni" class="py-20 bg-white">
    <div class="max-w-6xl mx-auto px-4">
        <div class="text-center mb-16 fade-in">
            <h2 class="text-4xl font-bold mb-4 bg-gradient-to-r from-yellow-600 to-orange-600 bg-clip-text text-transparent">
                Testimoni Jamaah
            </h2>
            <p class="text-xl text-gray-600">Kepuasan jamaah adalah prioritas utama kami</p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            @foreach(App\Models\Testimonial::active()->limit(6)->get() as $testimonial)
            <div class="fade-in">
                @php
                    $gradients = [
                        ['from' => 'from-yellow-50', 'to' => 'to-orange-50', 'avatarFrom' => 'from-yellow-400', 'avatarTo' => 'to-orange-400'],
                        ['from' => 'from-green-50', 'to' => 'to-blue-50', 'avatarFrom' => 'from-green-400', 'avatarTo' => 'to-blue-400'],
                    ];
                    $randomGradient = $gradients[array_rand($gradients)];
                @endphp

                <div class="bg-gradient-to-br {{ $randomGradient['from'] }} {{ $randomGradient['to'] }} p-8 rounded-2xl shadow-lg hover:shadow-xl transition-all duration-300">
                    <div class="flex items-center mb-4">
                        @if($testimonial->avatar)
                        <img src="{{ $testimonial->avatar_url }}" alt="{{ e($testimonial->name) }}" class="w-12 h-12 rounded-full object-cover">
                        @else
                        <div class="w-12 h-12 bg-gradient-to-br {{ $randomGradient['avatarFrom'] }} {{ $randomGradient['avatarTo'] }} rounded-full flex items-center justify-center">
                            <i class="fas fa-user text-white"></i>
                        </div>
                        @endif
                        <div class="ml-4">
                            <h4 class="font-semibold text-gray-800">{{ $testimonial->name }}</h4>
                            <p class="text-sm text-gray-600">{{ $testimonial->location }}</p>
                        </div>
                    </div>
                    <div class="flex mb-4 text-yellow-400">
                        {!! $testimonial->stars !!} <!-- Aman karena rating sudah di-cast sebagai integer -->
                    </div>
                    <p class="text-gray-700 italic">"{!! nl2br(e($testimonial->message)) !!}"</p>
                </div>
            </div>
            @endforeach
        </div>
    </div>
</section>





<!-- Features Section -->
<section class="py-20 gradient-bg">
  <div class="max-w-6xl mx-auto px-4">
    <div class="text-center mb-16 fade-in">
      <h2 class="text-4xl font-bold mb-4 text-white">
        Mengapa Pilih Mudaris Mandiri Wisata?
      </h2>
      <p class="text-xl text-white opacity-90">Keunggulan yang membuat kami berbeda</p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
      <div class="text-center text-white fade-in">
        <div class="w-20 h-20 bg-white bg-opacity-20 rounded-2xl flex items-center justify-center mx-auto mb-6 backdrop-blur-sm">
          <i class="fas fa-shield-alt text-3xl"></i>
        </div>
        <h3 class="text-xl font-semibold mb-3">Terpercaya</h3>
        <p class="opacity-90">Berizin resmi dan berpengalaman lebih dari 15 tahun</p>
      </div>

      <div class="text-center text-white fade-in">
        <div class="w-20 h-20 bg-white bg-opacity-20 rounded-2xl flex items-center justify-center mx-auto mb-6 backdrop-blur-sm">
          <i class="fas fa-hands text-3xl"></i>
        </div>
        <h3 class="text-xl font-semibold mb-3">Amanah</h3>
        <p class="opacity-90">Menjalankan ibadah dengan penuh tanggung jawab</p>
      </div>

      <div class="text-center text-white fade-in">
        <div class="w-20 h-20 bg-white bg-opacity-20 rounded-2xl flex items-center justify-center mx-auto mb-6 backdrop-blur-sm">
          <i class="fas fa-headset text-3xl"></i>
        </div>
        <h3 class="text-xl font-semibold mb-3">Pelayanan 24/7</h3>
        <p class="opacity-90">Siap melayani kebutuhan jamaah kapan saja</p>
      </div>

      <div class="text-center text-white fade-in">
        <div class="w-20 h-20 bg-white bg-opacity-20 rounded-2xl flex items-center justify-center mx-auto mb-6 backdrop-blur-sm">
          <i class="fas fa-heart text-3xl"></i>
        </div>
        <h3 class="text-xl font-semibold mb-3">Berkah</h3>
        <p class="opacity-90">Mengutamakan kenyamanan spiritual jamaah</p>
      </div>
    </div>
  </div>
</section>

<script>
document.addEventListener('DOMContentLoaded', function() {
    loadTestimonials();
});

function loadTestimonials() {
    fetch('/api/testimonials/active')
        .then(response => response.json())
        .then(data => {
            const container = document.getElementById('testimonials-container');
            const staticTestimonials = document.getElementById('static-testimonials');

            if (data.length > 0) {
                container.innerHTML = '';

                // Array warna gradient untuk variasi
                const gradientColors = [
                    'from-yellow-50 to-orange-50',
                    'from-green-50 to-blue-50',
                    'from-purple-50 to-pink-50',
                    'from-blue-50 to-indigo-50',
                    'from-pink-50 to-rose-50',
                    'from-indigo-50 to-purple-50'
                ];

                const avatarColors = [
                    'from-yellow-400 to-orange-400',
                    'from-green-400 to-blue-400',
                    'from-purple-400 to-pink-400',
                    'from-blue-400 to-indigo-400',
                    'from-pink-400 to-rose-400',
                    'from-indigo-400 to-purple-400'
                ];

                data.forEach((testimonial, index) => {
                    const colorIndex = index % gradientColors.length;
                    const testimonialHtml = `
                        <div class="fade-in">
                            <div class="bg-gradient-to-br ${gradientColors[colorIndex]} p-8 rounded-2xl shadow-lg hover:shadow-xl transition-all duration-300">
                                <div class="flex items-center mb-4">
                                    <div class="w-12 h-12 bg-gradient-to-br ${avatarColors[colorIndex]} rounded-full flex items-center justify-center overflow-hidden">
                                        ${testimonial.avatar_url.includes('ui-avatars.com') ?
                                            `<i class="fas fa-user text-white"></i>` :
                                            `<img src="${testimonial.avatar_url}" alt="${testimonial.name}" class="w-full h-full object-cover">`
                                        }
                                    </div>
                                    <div class="ml-4">
                                        <h4 class="font-semibold text-gray-800">${testimonial.name}</h4>
                                        <p class="text-sm text-gray-600">${testimonial.location}</p>
                                    </div>
                                </div>
                                <div class="flex mb-4">
                                    ${generateStars(testimonial.rating)}
                                </div>
                                <p class="text-gray-700 italic">"${testimonial.message}"</p>
                            </div>
                        </div>
                    `;
                    container.innerHTML += testimonialHtml;
                });
            } else {
                // Jika tidak ada data dari database, tampilkan static testimonials
                container.classList.add('hidden');
                staticTestimonials.classList.remove('hidden');
            }
        })
        .catch(error => {
            console.error('Error loading testimonials:', error);
            // Fallback ke static testimonials
            const container = document.getElementById('testimonials-container');
            const staticTestimonials = document.getElementById('static-testimonials');
            container.classList.add('hidden');
            staticTestimonials.classList.remove('hidden');
        });
}

function generateStars(rating) {
    let stars = '';
    for (let i = 1; i <= 5; i++) {
        if (i <= rating) {
            stars += '<i class="fas fa-star text-yellow-400"></i>';
        } else {
            stars += '<i class="far fa-star text-yellow-400"></i>';
        }
    }
    return stars;
}
</script>



<script>
document.addEventListener('DOMContentLoaded', function() {
    loadTestimonials();
});

function loadTestimonials() {
    fetch('/api/testimonials/active')
        .then(response => response.json())
        .then(data => {
            const container = document.getElementById('testimonials-container');
            const staticTestimonials = document.getElementById('static-testimonials');

            if (data.length > 0) {
                container.innerHTML = '';

                // Array warna gradient untuk variasi
                const gradientColors = [
                    'from-yellow-50 to-orange-50',
                    'from-green-50 to-blue-50',
                    'from-purple-50 to-pink-50',
                    'from-blue-50 to-indigo-50',
                    'from-pink-50 to-rose-50',
                    'from-indigo-50 to-purple-50'
                ];

                const avatarColors = [
                    'from-yellow-400 to-orange-400',
                    'from-green-400 to-blue-400',
                    'from-purple-400 to-pink-400',
                    'from-blue-400 to-indigo-400',
                    'from-pink-400 to-rose-400',
                    'from-indigo-400 to-purple-400'
                ];

                data.forEach((testimonial, index) => {
                    const colorIndex = index % gradientColors.length;
                    const testimonialHtml = `
                        <div class="fade-in">
                            <div class="bg-gradient-to-br ${gradientColors[colorIndex]} p-8 rounded-2xl shadow-lg hover:shadow-xl transition-all duration-300">
                                <div class="flex items-center mb-4">
                                    <div class="w-12 h-12 bg-gradient-to-br ${avatarColors[colorIndex]} rounded-full flex items-center justify-center overflow-hidden">
                                        ${testimonial.avatar_url.includes('ui-avatars.com') ?
                                            `<i class="fas fa-user text-white"></i>` :
                                            `<img src="${testimonial.avatar_url}" alt="${testimonial.name}" class="w-full h-full object-cover">`
                                        }
                                    </div>
                                    <div class="ml-4">
                                        <h4 class="font-semibold text-gray-800">${testimonial.name}</h4>
                                        <p class="text-sm text-gray-600">${testimonial.location}</p>
                                    </div>
                                </div>
                                <div class="flex mb-4">
                                    ${generateStars(testimonial.rating)}
                                </div>
                                <p class="text-gray-700 italic">"${testimonial.message}"</p>
                            </div>
                        </div>
                    `;
                    container.innerHTML += testimonialHtml;
                });
            } else {
                // Jika tidak ada data dari database, tampilkan static testimonials
                container.classList.add('hidden');
                staticTestimonials.classList.remove('hidden');
            }
        })
        .catch(error => {
            console.error('Error loading testimonials:', error);
            // Fallback ke static testimonials
            const container = document.getElementById('testimonials-container');
            const staticTestimonials = document.getElementById('static-testimonials');
            container.classList.add('hidden');
            staticTestimonials.classList.remove('hidden');
        });
}

function generateStars(rating) {
    let stars = '';
    for (let i = 1; i <= 5; i++) {
        if (i <= rating) {
            stars += '<i class="fas fa-star text-yellow-400"></i>';
        } else {
            stars += '<i class="far fa-star text-yellow-400"></i>';
        }
    }
    return stars;
}
</script>



  <!-- Kontak -->
  <section id="kontak" class="py-20 bg-gradient-to-br from-gray-50 to-white">
    <div class="max-w-6xl mx-auto px-4">
      <div class="text-center mb-16 fade-in">
        <h2 class="text-4xl font-bold mb-4 bg-gradient-to-r from-yellow-600 to-orange-600 bg-clip-text text-transparent">
          Hubungi Kami
        </h2>
        <p class="text-xl text-gray-600">Siap membantu mewujudkan impian umroh Anda</p>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12">
 <!-- Contact Info -->
<div class="fade-in">
  <div class="space-y-8">
    @php
        $contact = App\Models\Contact::getActiveContact();
    @endphp

    @if($contact)
    <div class="flex items-start space-x-4">
      <div class="w-12 h-12 gradient-bg rounded-xl flex items-center justify-center flex-shrink-0">
        <i class="fas fa-map-marker-alt text-white"></i>
      </div>
      <div>
        <h3 class="text-lg font-semibold text-gray-800 mb-2">Alamat Kantor</h3>
        <p class="text-gray-600">{{ $contact->office_address }}</p>
      </div>
    </div>

    <div class="flex items-start space-x-4">
      <div class="w-12 h-12 gradient-bg rounded-xl flex items-center justify-center flex-shrink-0">
        <i class="fas fa-phone text-white"></i>
      </div>
      <div>
        <h3 class="text-lg font-semibold text-gray-800 mb-2">Telepon</h3>
        @if($contact->phone_1)
          <p class="text-gray-600">{{ $contact->phone_1 }}</p>
        @endif
        @if($contact->phone_2)
          <p class="text-gray-600">{{ $contact->phone_2 }}</p>
        @endif
      </div>
    </div>

    <div class="flex items-start space-x-4">
      <div class="w-12 h-12 gradient-bg rounded-xl flex items-center justify-center flex-shrink-0">
        <i class="fas fa-envelope text-white"></i>
      </div>
      <div>
        <h3 class="text-lg font-semibold text-gray-800 mb-2">Email</h3>
        @if($contact->email_1)
          <p class="text-gray-600">{{ $contact->email_1 }}</p>
        @endif
        @if($contact->email_2)
          <p class="text-gray-600">{{ $contact->email_2 }}</p>
        @endif
      </div>
    </div>

    <div class="flex items-start space-x-4">
      <div class="w-12 h-12 gradient-bg rounded-xl flex items-center justify-center flex-shrink-0">
        <i class="fas fa-clock text-white"></i>
      </div>
      <div>
        <h3 class="text-lg font-semibold text-gray-800 mb-2">Jam Operasional</h3>
        @if($contact->monday_friday_hours)
          <p class="text-gray-600">Senin - Jumat: {{ $contact->monday_friday_hours }}</p>
        @endif
        @if($contact->saturday_hours)
          <p class="text-gray-600">Sabtu: {{ $contact->saturday_hours }}</p>
        @endif
      </div>
    </div>

    <!-- Social Media -->
    <div class="mt-12">
      <h3 class="text-lg font-semibold text-gray-800 mb-6">Ikuti Media Sosial Kami</h3>
      <div class="flex space-x-4">
        @if($contact->facebook_url)
        <a href="{{ $contact->facebook_url }}" target="_blank" class="w-12 h-12 bg-blue-600 rounded-xl flex items-center justify-center text-white hover:bg-blue-700 transition-colors">
          <i class="fab fa-facebook-f"></i>
        </a>
        @endif

        @if($contact->whatsapp_link)
        <a href="{{ $contact->whatsapp_link }}" target="_blank" class="w-12 h-12 bg-green-600 rounded-xl flex items-center justify-center text-white hover:bg-green-700 transition-colors">
          <i class="fab fa-whatsapp"></i>
        </a>
        @endif

        @if($contact->instagram_url)
        <a href="{{ $contact->instagram_url }}" target="_blank" class="w-12 h-12 bg-pink-600 rounded-xl flex items-center justify-center text-white hover:bg-pink-700 transition-colors">
          <i class="fab fa-instagram"></i>
        </a>
        @endif

        @if($contact->youtube_url)
        <a href="{{ $contact->youtube_url }}" target="_blank" class="w-12 h-12 bg-red-600 rounded-xl flex items-center justify-center text-white hover:bg-red-700 transition-colors">
          <i class="fab fa-youtube"></i>
        </a>
        @endif
      </div>
    </div>
    @else
      <div class="text-gray-600">Informasi kontak tidak tersedia</div>
    @endif
  </div>
</div>

<!-- Contact Form -->
<div class="fade-in">
  <div class="bg-white rounded-3xl shadow-xl p-8">
    <h3 class="text-2xl font-bold text-gray-800 mb-6">Konsultasi Gratis</h3>

    <!-- Alert untuk menampilkan pesan -->
    <div id="alert-message" class="hidden mb-4 p-4 rounded-xl"></div>

    <form id="contactForm" class="space-y-6">
      @csrf
      <div>
        <label class="block text-sm font-semibold text-gray-700 mb-2">Nama Lengkap *</label>
        <input type="text" name="full_name" id="full_name" required class="w-full px-4 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-yellow-500 focus:border-transparent transition-all duration-300" placeholder="Masukkan nama lengkap">
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div>
          <label class="block text-sm font-semibold text-gray-700 mb-2">Email *</label>
          <input type="email" name="email" id="email" required class="w-full px-4 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-yellow-500 focus:border-transparent transition-all duration-300" placeholder="email@example.com">
        </div>
        <div>
          <label class="block text-sm font-semibold text-gray-700 mb-2">No. Telepon *</label>
          <input type="tel" name="phone" id="phone" required class="w-full px-4 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-yellow-500 focus:border-transparent transition-all duration-300" placeholder="+62 812 3456 7890">
        </div>
      </div>

      <div>
        <label class="block text-sm font-semibold text-gray-700 mb-2">Paket yang Diminati *</label>
        <select name="package_interest" id="package_interest" required class="w-full px-4 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-yellow-500 focus:border-transparent transition-all duration-300">
          <option value="">Pilih Paket</option>
          <option value="Paket Reguler">Paket Reguler</option>
          <option value="Paket VIP">Paket VIP</option>
          <option value="Paket Keluarga">Paket Keluarga</option>
        </select>
      </div>

      <div>
        <label class="block text-sm font-semibold text-gray-700 mb-2">Pesan</label>
        <textarea name="message" id="message" rows="4" class="w-full px-4 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-yellow-500 focus:border-transparent transition-all duration-300" placeholder="Tulis pesan atau pertanyaan Anda..."></textarea>
      </div>

      <button type="submit" id="submitButton" class="w-full gradient-bg text-white py-4 rounded-xl font-semibold hover:shadow-lg transform hover:scale-105 transition-all duration-300">
        <span id="buttonText">
          <i class="fas fa-paper-plane mr-2"></i>
          Kirim Pesan
        </span>
        <span id="loadingText" class="hidden">
          <i class="fas fa-spinner fa-spin mr-2"></i>
          Mengirim...
        </span>
      </button>
    </form>
  </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const form = document.getElementById('contactForm');
    const submitButton = document.getElementById('submitButton');
    const buttonText = document.getElementById('buttonText');
    const loadingText = document.getElementById('loadingText');
    const alertMessage = document.getElementById('alert-message');

    form.addEventListener('submit', async function(e) {
        e.preventDefault();

        // Disable button dan tampilkan loading
        submitButton.disabled = true;
        buttonText.classList.add('hidden');
        loadingText.classList.remove('hidden');

        // Hide previous alerts
        alertMessage.classList.add('hidden');

        try {
            const formData = new FormData(form);

            const response = await fetch('/contact', {
                method: 'POST',
                body: formData,
                headers: {
                    'X-CSRF-TOKEN': document.querySelector('meta[name="csrf-token"]').getAttribute('content')
                }
            });

            const result = await response.json();

            if (result.success) {
                // Tampilkan pesan sukses
                showAlert('success', result.message);


            } else {
                showAlert('error', result.message);
            }

        } catch (error) {
            console.error('Error:', error);
            showAlert('error', 'Terjadi kesalahan sistem. Silakan coba lagi.');
        } finally {
            // Re-enable button
            submitButton.disabled = false;
            buttonText.classList.remove('hidden');
            loadingText.classList.add('hidden');
        }
    });

    function showAlert(type, message) {
        alertMessage.className = `mb-4 p-4 rounded-xl ${type === 'success' ? 'bg-green-100 text-green-700 border border-green-200' : 'bg-red-100 text-red-700 border border-red-200'}`;
        alertMessage.innerHTML = `
            <div class="flex items-center">
                <i class="fas ${type === 'success' ? 'fa-check-circle' : 'fa-exclamation-circle'} mr-2"></i>
                ${message}
            </div>
        `;
        alertMessage.classList.remove('hidden');

        // Scroll ke alert
        alertMessage.scrollIntoView({ behavior: 'smooth', block: 'center' });
    }
});
</script>

<!-- Pastikan menambahkan CSRF token di head -->
<meta name="csrf-token" content="{{ csrf_token() }}">
  </section>

  <!-- Footer -->
  <footer class="bg-gray-900 text-white py-12">
    <div class="max-w-6xl mx-auto px-4">
      <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
        <div>
<div class="flex-shrink-0 flex items-center space-x-3">
  <div class=" items-center justify-center">
    <img src="{{ asset('assets/images/logo.png') }}" alt="Logo" class="w-40 h-26 object-contain" />
  </div>
</div>
          <p class="text-gray-400 mb-6">Melayani perjalanan umroh dengan penuh amanah dan berkah sejak 2012.</p>
          <div class="flex space-x-4">
            <a href="#" class="w-8 h-8 bg-gray-700 rounded-lg flex items-center justify-center text-gray-300 hover:bg-yellow-600 hover:text-white transition-colors">
              <i class="fab fa-facebook-f text-sm"></i>
            </a>
            <a href="#" class="w-8 h-8 bg-gray-700 rounded-lg flex items-center justify-center text-gray-300 hover:bg-yellow-600 hover:text-white transition-colors">
              <i class="fab fa-whatsapp text-sm"></i>
            </a>
            <a href="#" class="w-8 h-8 bg-gray-700 rounded-lg flex items-center justify-center text-gray-300 hover:bg-yellow-600 hover:text-white transition-colors">
              <i class="fab fa-instagram text-sm"></i>
            </a>
          </div>
        </div>

        <div>
          <h4 class="text-lg font-semibold mb-4">Layanan</h4>
          <ul class="space-y-2 text-gray-400">
            <li><a href="#paket" class="hover:text-white transition-colors">Umroh Reguler</a></li>
            <li><a href="#paket" class="hover:text-white transition-colors">Umroh VIP</a></li>
            <li><a href="#paket" class="hover:text-white transition-colors">Umroh Keluarga</a></li>
            <li><a href="#kontak" class="hover:text-white transition-colors">Konsultasi</a></li>
          </ul>
        </div>

        <div>
          <h4 class="text-lg font-semibold mb-4">Informasi</h4>
          <ul class="space-y-2 text-gray-400">
            <li><a href="/tentang-kami" class="hover:text-white transition-colors">Tentang Kami</a></li>
            <li><a href="/syarat-ketentuan" class="hover:text-white transition-colors">Syarat & Ketentuan</a></li>
            <li><a href="/kebijakan-privasi" class="hover:text-white transition-colors">Kebijakan Privasi</a></li>
            <li><a href="/faq" class="hover:text-white transition-colors">FAQ</a></li>
          </ul>
        </div>

        <div>
          <h4 class="text-lg font-semibold mb-4">Kontak</h4>
          <ul class="space-y-2 text-gray-400">
            <li class="flex items-center">
              <i class="fas fa-phone mr-2"></i>
              +62 852-1145-1111
            </li>
            <li class="flex items-center">
              <i class="fas fa-envelope mr-2"></i>
              mudariswisata@gmail.com
            </li>
            <li class="flex items-center">
              <i class="fas fa-map-marker-alt mr-2"></i>
              Pekanbaru, Riau
            </li>
          </ul>
        </div>
      </div>

      <div class="border-t border-gray-700 mt-8 pt-8 text-center text-gray-400">
        <p>&copy; 2012 - Mundaris Mandiri Wisata ❤️ for jamaah Indonesia.</p>
      </div>
    </div>
  </footer>

  <!-- Floating WhatsApp Button -->
  <div class="fixed bottom-6 right-6 z-50">
    <a href="https://wa.me/6285211451111" target="_blank" class="w-14 h-14 bg-green-500 hover:bg-green-600 rounded-full flex items-center justify-center text-white shadow-lg hover:shadow-xl transition-all duration-300 floating-animation">
      <i class="fab fa-whatsapp text-2xl"></i>
    </a>
  </div>

<script>
    document.addEventListener('DOMContentLoaded', function() {

    // ============ GUIDE NOTIFICATION SYSTEM ============
    let guideNotificationCount = 0;
    let isFirstVisit = !sessionStorage.getItem('hasVisited');
    let hasShownScrollGuide = false;
    let hasShownMenuGuide = false;
    let hasShownFormGuide = false;

    // Initialize user guide
    setTimeout(() => {
        if (isFirstVisit) {
            showGuideNotification('👋 Selamat datang di Mudaris Mandiri Wisata! Scroll ke bawah untuk melihat paket umroh terbaik kami.', 'welcome', 3000);
            sessionStorage.setItem('hasVisited', 'true');
            isFirstVisit = false;
        }
    }, 1000);

    // Track shown section guides
    const shownSectionGuides = JSON.parse(sessionStorage.getItem('shownSectionGuides') || '{}');

    // ============ MOBILE MENU FUNCTIONALITY ============
    const menuBtn = document.getElementById('menu-btn');
    const mobileMenu = document.getElementById('mobile-menu');
    const mobileMenuIcon = menuBtn.querySelector('i');

    // Toggle mobile menu
    if (menuBtn) {
        menuBtn.addEventListener('click', function() {
            mobileMenu.classList.toggle('hidden');
            mobileMenu.classList.toggle('active');

            // Show guide for first time menu usage
            if (!hasShownMenuGuide) {
                showGuideNotification('📱 Menu mobile telah dibuka! Klik di luar menu atau tombol ✕ untuk menutup.', 'guide');
                hasShownMenuGuide = true;
            }

            // Toggle icon
            if (mobileMenu.classList.contains('active')) {
                mobileMenuIcon.className = 'fas fa-times text-2xl mobile-menu-icon active';
                showGuideNotification('📋 Pilih menu yang ingin Anda kunjungi', 'info', 2000);
            } else {
                mobileMenuIcon.className = 'fas fa-bars text-2xl mobile-menu-icon';
                showGuideNotification('✅ Menu ditutup', 'success', 1500);
            }
        });
    }

    // Close mobile menu when clicking on menu links
    const mobileMenuLinks = mobileMenu?.querySelectorAll('a');
    mobileMenuLinks?.forEach(link => {
        link.addEventListener('click', function() {
            mobileMenu.classList.add('hidden');
            mobileMenu.classList.remove('active');
            mobileMenuIcon.className = 'fas fa-bars text-2xl mobile-menu-icon';

            const sectionName = this.textContent.trim();
            showGuideNotification(`🎯 Menuju ke bagian ${sectionName}...`, 'info', 2000);
        });
    });

    // Close mobile menu when clicking outside
    document.addEventListener('click', function(e) {
        if (mobileMenu && !menuBtn?.contains(e.target) && !mobileMenu.contains(e.target)) {
            if (mobileMenu.classList.contains('active')) {
                mobileMenu.classList.add('hidden');
                mobileMenu.classList.remove('active');
                mobileMenuIcon.className = 'fas fa-bars text-2xl mobile-menu-icon';
                showGuideNotification('👆 Menu ditutup karena Anda klik di luar area', 'info', 2000);
            }
        }
    });

    // ============ NAVBAR SCROLL EFFECT ============
    const navbar = document.getElementById('navbar');
    let lastScrollTop = 0;

    window.addEventListener('scroll', throttle(function() {
        const scrollTop = window.pageYOffset || document.documentElement.scrollTop;

        // Show scroll guide notification
        if (scrollTop > 50 && !hasShownScrollGuide) {
            showGuideNotification('🔄 Navbar akan berubah transparan saat Anda scroll! Coba scroll lebih banyak.', 'guide');
            hasShownScrollGuide = true;
        }

        // Change navbar background on scroll
        if (navbar) {
            if (scrollTop > 100) {
                navbar.style.background = 'rgba(255, 255, 255, 0.95)';
                navbar.style.backdropFilter = 'blur(20px)';
                navbar.style.boxShadow = '0 8px 32px 0 rgba(31, 38, 135, 0.37)';
            } else {
                navbar.style.background = 'rgba(255, 255, 255, 0.25)';
                navbar.style.backdropFilter = 'blur(10px)';
                navbar.style.boxShadow = 'none';
            }

            // Hide/show navbar on scroll
            if (scrollTop > lastScrollTop && scrollTop > 200) {
                navbar.style.transform = 'translateY(-100%)';
            } else {
                navbar.style.transform = 'translateY(0)';
            }
        }

        lastScrollTop = scrollTop;
    }, 100));

    // ============ SMOOTH SCROLLING FOR NAVIGATION LINKS ============
    const navLinks = document.querySelectorAll('a[href^="#"]');

    navLinks.forEach(link => {
        link.addEventListener('click', function(e) {
            e.preventDefault();

            const targetId = this.getAttribute('href');
            const targetSection = document.querySelector(targetId);
            const linkText = this.textContent.trim();

            if (targetSection) {
                const navbarHeight = navbar?.offsetHeight || 0;
                const targetPosition = targetSection.offsetTop - navbarHeight - 20;

                window.scrollTo({
                    top: targetPosition,
                    behavior: 'smooth'
                });

                showGuideNotification(`🎯 Menuju ke bagian ${linkText}...`, 'info', 2000);
            }
        });
    });

    // ============ SCROLL REVEAL ANIMATION ============
    const observerOptions = {
        threshold: 0.1,
        rootMargin: '0px 0px -50px 0px'
    };

    const observer = new IntersectionObserver(function(entries) {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.classList.add('visible');

                // Show section-specific guides
                const sectionId = entry.target.id || entry.target.closest('[id]')?.id;
                showSectionGuide(sectionId);
            }
        });
    }, observerOptions);

    // Observe all fade-in elements
    const fadeElements = document.querySelectorAll('.fade-in');
    fadeElements.forEach(el => observer.observe(el));

    // ============ ANIMATED COUNTER FOR STATS ============
    const animateCounter = (element, target, duration = 2000) => {
        let start = 0;
        const increment = target / (duration / 16);

        const timer = setInterval(() => {
            start += increment;
            if (start >= target) {
                element.textContent = target + (element.textContent.includes('+') ? '+' : '') +
                                   (element.textContent.includes('%') ? '%' : '');
                clearInterval(timer);
            } else {
                element.textContent = Math.floor(start) + (element.textContent.includes('+') ? '+' : '') +
                                   (element.textContent.includes('%') ? '%' : '');
            }
        }, 16);
    };

    // Animate stats when they come into view
    const statsSection = document.querySelector('.gradient-bg');
    const statsObserver = new IntersectionObserver(function(entries) {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                const statNumbers = entry.target.querySelectorAll('.text-4xl');

                statNumbers.forEach(stat => {
                    const text = stat.textContent;
                    const number = parseInt(text.replace(/\D/g, ''));
                    if (number) {
                        stat.textContent = '0' + (text.includes('+') ? '+' : '') +
                                         (text.includes('%') ? '%' : '');
                        animateCounter(stat, number);
                    }
                });

                showGuideNotification('📊 Lihat statistik kami yang menakjubkan!', 'info', 2500);
                statsObserver.unobserve(entry.target);
            }
        });
    }, { threshold: 0.5 });

    if (statsSection) {
        statsObserver.observe(statsSection);
    }

    // ============ PAKET UMROH BUTTON HANDLERS ============
    const paketBtns = Array.from(document.querySelectorAll('button')).filter(btn =>
        btn.textContent.includes('Pilih Paket')
    );

    paketBtns.forEach((btn, index) => {
        // Add hover guide
        btn.addEventListener('mouseenter', function() {
            showGuideNotification('👆 Klik untuk memilih paket dan mendapat konsultasi gratis!', 'guide', 2000);
        });

        btn.addEventListener('click', function() {
            const paketTypes = ['Reguler', 'VIP', 'Keluarga'];
            const paketType = paketTypes[index] || 'Umroh';

            showNotification(`✅ Anda memilih Paket ${paketType}. Tim kami akan menghubungi Anda segera!`, 'success');
            showGuideNotification('📝 Sekarang lengkapi form kontak di bawah untuk mendapat informasi lebih lanjut!', 'guide', 4000);

            // Scroll to contact form
            setTimeout(() => {
                document.querySelector('#kontak')?.scrollIntoView({
                    behavior: 'smooth',
                    block: 'start'
                });
            }, 1000);
        });
    });

    // ============ CONTACT FORM HANDLING ============
    const contactForm = document.querySelector('form');

    if (contactForm) {
        // Show form guide when user focuses on first input
        const firstInput = contactForm.querySelector('input');
        if (firstInput) {
            firstInput.addEventListener('focus', function() {
                if (!hasShownFormGuide) {
                    showGuideNotification('📝 Lengkapi semua field yang diperlukan untuk mendapat respon cepat dari tim kami!', 'guide', 4000);
                    hasShownFormGuide = true;
                }
            });
        }

        contactForm.addEventListener('submit', function(e) {
            e.preventDefault();

            // Get form data
            const name = this.querySelector('input[placeholder*="nama"]')?.value;
            const email = this.querySelector('input[type="email"]')?.value;
            const phone = this.querySelector('input[type="tel"]')?.value;
            const paket = this.querySelector('select')?.value;
            const message = this.querySelector('textarea')?.value;

            // Basic validation
            if (!name || !email || !phone) {
                showNotification('❌ Mohon lengkapi data yang diperlukan!', 'error');
                showGuideNotification('💡 Tip: Nama, email, dan nomor HP wajib diisi untuk proses yang lebih cepat.', 'guide', 3000);
                return;
            }

            if (!isValidEmail(email)) {
                showNotification('❌ Format email tidak valid!', 'error');
                showGuideNotification('📧 Contoh email yang benar: nama@email.com', 'guide', 3000);
                return;
            }

            // Simulate form submission
            const submitBtn = this.querySelector('button[type="submit"]');
            const originalText = submitBtn?.innerHTML;

            if (submitBtn) {
                submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Mengirim...';
                submitBtn.disabled = true;
            }

            showGuideNotification('⏳ Sedang mengirim pesan Anda...', 'info', 2000);

            setTimeout(() => {
                showNotification('✅ Pesan berhasil dikirim! Tim kami akan menghubungi Anda segera.', 'success');
                showGuideNotification('🎉 Terima kasih! Kami akan menghubungi Anda dalam 1x24 jam. Jangan lupa cek WhatsApp!', 'welcome', 5000);
                this.reset();
                if (submitBtn && originalText) {
                    submitBtn.innerHTML = originalText;
                    submitBtn.disabled = false;
                }
            }, 2000);
        });
    }

    // ============ GALLERY IMAGE MODAL ============
    const galleryImages = document.querySelectorAll('#galeri img');

    galleryImages.forEach((img, index) => {
        img.addEventListener('mouseenter', function() {
            showGuideNotification('🖼️ Klik gambar untuk melihat dalam ukuran penuh!', 'guide', 2000);
        });

        img.addEventListener('click', function() {
            createImageModal(this.src, this.alt);
            showGuideNotification('🔍 Gunakan tombol ESC atau klik di luar gambar untuk menutup.', 'info', 3000);
        });

        img.style.cursor = 'pointer';
    });

    // ============ FLOATING WHATSAPP BUTTON ============
    const whatsappBtn = document.querySelector('.floating-animation a[href*="wa.me"]');

    if (whatsappBtn) {
        whatsappBtn.addEventListener('mouseenter', function() {
            showGuideNotification('💬 Klik untuk chat langsung via WhatsApp!', 'guide', 2000);
        });

        whatsappBtn.addEventListener('click', function(e) {
            this.style.transform = 'scale(0.95)';
            showGuideNotification('📱 Membuka WhatsApp... Siap untuk konsultasi gratis!', 'success', 2000);
            setTimeout(() => {
                this.style.transform = 'scale(1)';
            }, 150);
        });
    }

    // ============ HERO BUTTONS FUNCTIONALITY ============
    const heroButtons = document.querySelectorAll('#home button');

    heroButtons.forEach(btn => {
        btn.addEventListener('mouseenter', function() {
            if (this.textContent.includes('Paket')) {
                showGuideNotification('📦 Lihat berbagai paket umroh yang tersedia!', 'guide', 2000);
            } else if (this.textContent.includes('Hubungi')) {
                showGuideNotification('📞 Hubungi kami untuk konsultasi gratis!', 'guide', 2000);
            }
        });

        btn.addEventListener('click', function() {
            if (this.textContent.includes('Paket')) {
                showGuideNotification('🎯 Menuju ke bagian paket umroh...', 'info', 2000);
                document.querySelector('#paket')?.scrollIntoView({
                    behavior: 'smooth',
                    block: 'start'
                });
            } else if (this.textContent.includes('Hubungi')) {
                showGuideNotification('📝 Menuju ke form kontak...', 'info', 2000);
                document.querySelector('#kontak')?.scrollIntoView({
                    behavior: 'smooth',
                    block: 'start'
                });
            }
        });
    });

    // ============ KONSULTASI GRATIS BUTTONS ============
    const konsultasiButtons = Array.from(document.querySelectorAll('button')).filter(btn =>
        btn.textContent.includes('Konsultasi Gratis')
    );

    konsultasiButtons.forEach(btn => {
        btn.addEventListener('mouseenter', function() {
            showGuideNotification('💡 Konsultasi gratis dengan expert umroh kami!', 'guide', 2000);
        });

        btn.addEventListener('click', function() {
            showGuideNotification('📞 Menuju form konsultasi gratis...', 'info', 2000);
            document.querySelector('#kontak')?.scrollIntoView({
                behavior: 'smooth',
                block: 'start'
            });
        });
    });

    // ============ FORM INPUT ENHANCEMENTS ============
    const formInputs = document.querySelectorAll('input, textarea, select');

    formInputs.forEach(input => {
        input.addEventListener('focus', function() {
            this.parentElement?.classList.add('focused');

            // Show specific input guides
            const placeholder = this.placeholder?.toLowerCase() || this.name?.toLowerCase() || '';
            if (placeholder.includes('nama')) {
                showGuideNotification('👤 Masukkan nama lengkap Anda', 'guide', 2000);
            } else if (placeholder.includes('email')) {
                showGuideNotification('📧 Gunakan email aktif untuk komunikasi', 'guide', 2000);
            } else if (placeholder.includes('phone') || placeholder.includes('hp')) {
                showGuideNotification('📱 Nomor HP untuk WhatsApp lebih diutamakan', 'guide', 2000);
            } else if (this.tagName.toLowerCase() === 'select') {
                showGuideNotification('📋 Pilih paket yang Anda minati', 'guide', 2000);
            } else if (this.tagName.toLowerCase() === 'textarea') {
                showGuideNotification('💬 Ceritakan kebutuhan umroh Anda secara detail', 'guide', 3000);
            }
        });

        input.addEventListener('blur', function() {
            this.parentElement?.classList.remove('focused');
        });

        if (input.value) {
            input.parentElement?.classList.add('has-value');
        }

        input.addEventListener('input', function() {
            if (this.value) {
                this.parentElement?.classList.add('has-value');
            } else {
                this.parentElement?.classList.remove('has-value');
            }
        });
    });

    // ============ SECTION-SPECIFIC GUIDES ============
    function showSectionGuide(sectionId) {
        const guides = {
            'home': '🏠 Selamat datang! Scroll ke bawah untuk melihat layanan kami.',
            'tentang': '🏢 Pelajari lebih lanjut tentang pengalaman dan komitmen kami.',
            'paket': '📦 Pilih paket umroh yang sesuai dengan kebutuhan Anda.',
            'galeri': '🖼️ Lihat dokumentasi perjalanan umroh bersama kami.',
            'testimonial': '⭐ Baca pengalaman jamaah yang telah berangkat bersama kami.',
            'kontak': '📞 Hubungi kami untuk konsultasi gratis dan informasi lebih lanjut.'
        };

        if (guides[sectionId] && !shownSectionGuides[sectionId]) {
            setTimeout(() => {
                showGuideNotification(guides[sectionId], 'guide', 3000);
                shownSectionGuides[sectionId] = true;
                sessionStorage.setItem('shownSectionGuides', JSON.stringify(shownSectionGuides));
            }, 500);
        }
    }

});

// ============ ENHANCED UTILITY FUNCTIONS ============

// Email validation
function isValidEmail(email) {
    const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    return emailRegex.test(email);
}

// Enhanced notification system
function showNotification(message, type = 'info', duration = 5000) {
    const existingNotification = document.querySelector('.notification');
    if (existingNotification) {
        existingNotification.remove();
    }

    const notification = document.createElement('div');
    notification.className = `notification fixed top-24 right-4 z-50 p-4 rounded-xl shadow-lg transform translate-x-full transition-all duration-300 max-w-sm`;

    const colors = {
        success: 'bg-green-500 text-white border-l-4 border-green-700',
        error: 'bg-red-500 text-white border-l-4 border-red-700',
        info: 'bg-blue-500 text-white border-l-4 border-blue-700',
        warning: 'bg-yellow-500 text-black border-l-4 border-yellow-700'
    };

    notification.className += ` ${colors[type] || colors.info}`;

    notification.innerHTML = `
        <div class="flex items-start space-x-3">
            <i class="fas fa-${getIconByType(type)} mt-1"></i>
            <div class="flex-1">
                <span class="text-sm font-medium">${message}</span>
            </div>
            <button onclick="this.closest('.notification').remove()" class="ml-2 hover:opacity-70 transition-opacity">
                <i class="fas fa-times text-sm"></i>
            </button>
        </div>
    `;

    document.body.appendChild(notification);

    setTimeout(() => {
        notification.style.transform = 'translateX(0)';
    }, 100);

    setTimeout(() => {
        if (notification.parentElement) {
            notification.style.transform = 'translateX(100%)';
            setTimeout(() => {
                notification.remove();
            }, 300);
        }
    }, duration);
}

// Guide notification system (positioned differently)
function showGuideNotification(message, type = 'guide', duration = 4000) {
    const existingGuide = document.querySelector('.guide-notification');
    if (existingGuide) {
        existingGuide.remove();
    }

    const guide = document.createElement('div');
    guide.className = `guide-notification fixed top-24 left-4 z-40 p-4 rounded-xl shadow-lg transform -translate-x-full transition-all duration-300 max-w-xs`;

    const colors = {
        guide: 'bg-gradient-to-r from-yellow-400 to-orange-500 text-white border-l-4 border-yellow-600',
        welcome: 'bg-gradient-to-r from-purple-500 to-pink-500 text-white border-l-4 border-purple-700',
        info: 'bg-gradient-to-r from-blue-400 to-cyan-500 text-white border-l-4 border-blue-600'
    };

    guide.className += ` ${colors[type] || colors.guide}`;

    const icons = {
        guide: 'lightbulb',
        welcome: 'hand-peace',
        info: 'info-circle'
    };

    guide.innerHTML = `
        <div class="flex items-start space-x-3">
            <i class="fas fa-${icons[type] || icons.guide} mt-1 text-lg"></i>
            <div class="flex-1">
                <div class="text-xs font-bold opacity-80 mb-1">
                    ${type === 'welcome' ? 'SELAMAT DATANG' : type === 'info' ? 'INFO' : 'PANDUAN'}
                </div>
                <span class="text-sm font-medium leading-relaxed">${message}</span>
            </div>
            <button onclick="this.closest('.guide-notification').remove()" class="ml-2 hover:opacity-70 transition-opacity">
                <i class="fas fa-times text-sm"></i>
            </button>
        </div>
        <div class="mt-2 w-full bg-white bg-opacity-20 rounded-full h-1">
            <div class="guide-progress bg-white rounded-full h-1 transition-all duration-${duration}" style="width: 0%"></div>
        </div>
    `;

    document.body.appendChild(guide);

    setTimeout(() => {
        guide.style.transform = 'translateX(0)';
        // Start progress bar
        const progressBar = guide.querySelector('.guide-progress');
        if (progressBar) {
            setTimeout(() => {
                progressBar.style.width = '100%';
            }, 100);
        }
    }, 100);

    setTimeout(() => {
        if (guide.parentElement) {
            guide.style.transform = 'translateX(-100%)';
            setTimeout(() => {
                guide.remove();
            }, 300);
        }
    }, duration);
}

// Get icon by notification type
function getIconByType(type) {
    const icons = {
        success: 'check-circle',
        error: 'exclamation-circle',
        info: 'info-circle',
        warning: 'exclamation-triangle'
    };
    return icons[type] || icons.info;
}

// Enhanced image modal with guide
function createImageModal(src, alt) {
    const existingModal = document.querySelector('.image-modal');
    if (existingModal) {
        existingModal.remove();
    }

    const modal = document.createElement('div');
    modal.className = 'image-modal fixed inset-0 z-50 flex items-center justify-center bg-black bg-opacity-75 opacity-0 transition-opacity duration-300';

    modal.innerHTML = `
        <div class="relative max-w-4xl max-h-full p-4">
            <img src="${src}" alt="${alt}" class="max-w-full max-h-full object-contain rounded-lg shadow-2xl">
            <button onclick="this.closest('.image-modal').remove()"
                    class="absolute top-2 right-2 w-10 h-10 bg-white bg-opacity-20 rounded-full flex items-center justify-center text-white hover:bg-opacity-30 transition-all">
                <i class="fas fa-times"></i>
            </button>
            <div class="absolute bottom-4 left-4 right-4 text-center">
                <p class="text-white text-lg font-semibold bg-black bg-opacity-50 rounded-lg px-4 py-2 inline-block">
                    ${alt}
                </p>
            </div>
        </div>
    `;

    modal.addEventListener('click', function(e) {
        if (e.target === this) {
            this.remove();
        }
    });

    document.addEventListener('keydown', function(e) {
        if (e.key === 'Escape') {
            modal.remove();
        }
    });

    document.body.appendChild(modal);
    setTimeout(() => {
        modal.style.opacity = '1';
    }, 10);
}

// Smooth scroll to top function
function scrollToTop() {
    window.scrollTo({
        top: 0,
        behavior: 'smooth'
    });
    showGuideNotification('⬆️ Kembali ke atas halaman', 'info', 2000);
}

// Enhanced scroll to top button with guide
window.addEventListener('scroll', throttle(function() {
    let scrollTopBtn = document.querySelector('.scroll-top-btn');

    if (window.pageYOffset > 300) {
        if (!scrollTopBtn) {
            scrollTopBtn = document.createElement('button');
            scrollTopBtn.className = 'scroll-top-btn fixed bottom-24 right-6 w-12 h-12 bg-yellow-600 hover:bg-yellow-700 rounded-full flex items-center justify-center text-white shadow-lg z-40 transform scale-0 transition-all duration-300';
            scrollTopBtn.innerHTML = '<i class="fas fa-arrow-up"></i>';
            scrollTopBtn.title = 'Kembali ke atas';

            scrollTopBtn.addEventListener('mouseenter', function() {
                showGuideNotification('⬆️ Klik untuk kembali ke atas halaman', 'guide', 2000);
            });

            scrollTopBtn.onclick = scrollToTop;
            document.body.appendChild(scrollTopBtn);
        }
        setTimeout(() => {
            scrollTopBtn.style.transform = 'scale(1)';
        }, 100);
    } else if (scrollTopBtn) {
        scrollTopBtn.style.transform = 'scale(0)';
        setTimeout(() => {
            if (scrollTopBtn.parentElement) {
                scrollTopBtn.remove();
            }
        }, 300);
    }
}, 100));

// ============ LOADING ANIMATION ============
window.addEventListener('load', function() {
    const loader = document.querySelector('.loader');
    if (loader) {
        loader.style.opacity = '0';
        setTimeout(() => {
            loader.remove();
        }, 500);
    }

    const heroContent = document.querySelector('#home .fade-in');
    if (heroContent) {
        heroContent.classList.add('visible');
    }

    // Show welcome message after page load
    setTimeout(() => {
        showGuideNotification('🎉 Website telah dimuat! Jelajahi semua fitur kami dengan panduan yang tersedia.', 'welcome', 4000);
    }, 1500);
});

// ============ PERFORMANCE OPTIMIZATION ============
function debounce(func, wait) {
    let timeout;
    return function executedFunction(...args) {
        const later = () => {
            clearTimeout(timeout);
            func(...args);
        };
        clearTimeout(timeout);
        timeout = setTimeout(later, wait);
    };
}

function throttle(func, limit) {
    let inThrottle;
    return function() {
        const args = arguments;
        const context = this;
        if (!inThrottle) {
            func.apply(context, args);
            inThrottle = true;
            setTimeout(() => inThrottle = false, limit);
        }
    }
}
</script>


</body>
</html>
